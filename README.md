# Fogo Incinerator Data Repo

This repository serves as an external database and image host for the Fogo Incinerator dApp. 
It allows dynamic updates to the token blacklists, scam lists, and promotional banners via `tokendata.json` without needing to rebuild or redeploy the frontend.
